{
  "name": "vsc-snippet-catalog",
  "displayName": "Command Vault",
  "description": "VS Code extension scaffold for the Command Vault MVP.",
  "version": "0.0.1",
  "publisher": "albertmacmini",
  "private": true,
  "engines": {
    "vscode": "^1.96.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onView:commandVault.commands",
    "onCommand:commandVault.createCommand",
    "onCommand:commandVault.copyCommand",
    "onCommand:commandVault.editCommand",
    "onCommand:commandVault.deleteCommand",
    "onCommand:commandVault.runCommand",
    "onCommand:commandVault.searchCommands",
    "onCommand:commandVault.searchCommands.alternateExecution"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "commandVault",
          "title": "Command Vault",
          "icon": "resources/command-vault.svg"
        }
      ]
    },
    "views": {
      "commandVault": [
        {
          "id": "commandVault.commands",
          "name": "Commands"
        }
      ]
    },
    "commands": [
      {
        "command": "commandVault.createCommand",
        "title": "Command Vault: Create Command"
      },
      {
        "command": "commandVault.editCommand",
        "title": "Command Vault: Edit Command"
      },
      {
        "command": "commandVault.copyCommand",
        "title": "Command Vault: Copy Command"
      },
      {
        "command": "commandVault.runCommand",
        "title": "Command Vault: Run Command"
      },
      {
        "command": "commandVault.deleteCommand",
        "title": "Command Vault: Delete Command"
      },
      {
        "command": "commandVault.searchCommands",
        "title": "Command Vault: Search Commands"
      }
    ],
    "keybindings": [
      {
        "command": "commandVault.searchCommands.alternateExecution",
        "key": "alt+enter",
        "when": "inQuickOpen && commandVault.searchCommandsVisible"
      },
      {
        "command": "commandVault.searchCommands.edit",
        "key": "ctrl+enter",
        "mac": "cmd+enter",
        "when": "inQuickOpen && commandVault.searchCommandsVisible"
      }
    ],
    "configuration": {
      "title": "Command Vault",
      "properties": {
        "commandVault.defaultExecutionBehavior": {
          "type": "string",
          "default": "run",
          "enum": [
            "run",
            "paste"
          ],
          "markdownDescription": "Controls the default quick-pick execution action. `run` sends the command and executes it on Enter, while `paste` sends the command without a trailing newline on Enter."
        },
        "commandVault.enableGlobalScope": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Show and allow Command Vault global commands."
        },
        "commandVault.enableWorkspaceScope": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Show and allow Command Vault workspace commands."
        }
      }
    }
  },
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "compile": "npm run build",
    "clean": "rm -rf out coverage",
    "test:integration": "node ./test/integration/run-tests.mjs",
    "test": "node --test ./test/**/*.test.js",
    "test:vitest": "node --no-warnings --test --experimental-strip-types ./src/extension.test.ts ./src/command-vault/model.test.ts ./src/command-vault/repository.test.ts ./src/command-vault/execution.test.ts ./src/command-vault/create-command.test.ts ./src/command-vault/edit-delete-command.test.ts ./src/command-vault/search-command.test.ts ./src/command-vault/settings.test.ts ./src/command-vault/sidebar.test.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.1",
    "@types/vscode": "^1.101.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.7"
  }
}
